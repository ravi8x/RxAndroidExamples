<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.rxandroidexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".basics.Example1Activity" />
        <activity android:name=".basics.Example2Activity" />
        <activity android:name=".basics.Example3Activity">

        </activity>
        <activity android:name=".basics.Example4Activity" />
        <activity android:name=".observers.ObservableActivity">

        </activity>
        <activity android:name=".observers.FlowableObserverActivity" />
        <activity android:name=".observers.SingleObserverActivity"></activity>
        <activity android:name=".observers.MaybeObserverActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".observers.CompletableObserverActivity"></activity>
    </application>

</manifest>